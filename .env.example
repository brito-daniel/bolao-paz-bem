# Supabase — projeto criado em https://supabase.com/dashboard
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Usada apenas no servidor (scripts de importação, migrations). NUNCA expor no client.
SUPABASE_SERVICE_ROLE_KEY=

# String de conexão Postgres direta (Supabase > Project Settings > Database > Connection string > URI)
# Usada pelo drizzle-kit para gerar/aplicar migrations.
DATABASE_URL=
